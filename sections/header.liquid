<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ 'component-list-menu.css' | asset_url }}" media="print" onload="this.media='all'">
    <link rel="stylesheet" href="{{ 'component-menu-drawer.css' | asset_url }}" media="print" onload="this.media='all'">
    <link rel="stylesheet" href="{{ 'component-cart-notification.css' | asset_url }}" media="print" onload="this.media='all'">
    <style>
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
            background-color: #fff; /* Example background color */
        }

        header-drawer {
            margin-left: auto; /* Move hamburger drawer to the right */
        }

        .header__icons {
            display: flex;
            justify-content: space-between; /* Space between icons */
            align-items: center; /* Center icons vertically */
        }

        .header__icon--search {
            display: none; /* Hide the search icon */
        }
    </style>
    <title>Header Example</title>
</head>
<body>

<header class="header">
    <div class="header__heading-logo-wrapper">
        <img src="logo.png" alt="Logo" class="header__logo"> <!-- Your logo image -->
    </div>

    {%- if section.settings.menu != blank -%}
        <header-drawer>
            <!-- Hamburger icon rendering code goes here -->
            <button class="hamburger-button">☰</button>
        </header-drawer>
    {%- endif -%}

    <div class="header__icons">
        {%- if shop.customer_accounts_enabled -%}
            <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}" class="header__icon header__icon--account link focus-inset{% if section.settings.menu != blank %} small-hide{% endif %}">
                <!-- Account icon rendering code -->
                <span>Account</span>
            </a>
        {%- endif -%}

        <a href="{{ routes.cart_url }}" class="header__icon header__icon--cart link focus-inset cart-button" id="cart-icon-bubble">
            <span class="cart-text">Cart </span>
            <span class="cart-item-count">
                {% if cart.item_count > 0 %}
                    ({{ cart.item_count }})
                {% endif %}
            </span>
        </a>
    </div>
</header>

</body>
</html>