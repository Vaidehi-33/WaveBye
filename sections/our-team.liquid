<!-- Our Team Section -->
<section class="our-team-section">
  <div class="container">
    <!-- Section Title and Description -->
    <div class="section-header">
      <h2>{{ section.settings.team_title }}</h2>
      <p>{{ section.settings.team_description }}</p>
    </div>

    <!-- Team Members -->
    <div class="team-members">
      {% for member in section.blocks %}
        <div class="team-member">
          <img src="{{ member.settings.image | img_url: 'large' }}" alt="{{ member.settings.name }}">
          <h3>{{ member.settings.name }}</h3>
          <p>{{ member.settings.job_title }}</p>
          <p>{{ member.settings.description }}</p>
          <!-- Social Icons -->
          <div class="social-icons">
            {% if member.settings.linkedin %}
              <a href="{{ member.settings.linkedin }}" target="_blank"><i class="fab fa-linkedin"></i></a>
            {% endif %}
            {% if member.settings.facebook %}
              <a href="{{ member.settings.facebook }}" target="_blank"><i class="fab fa-facebook"></i></a>
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<style>
.our-team-section {
  padding: 50px 0;
}
.section-header h2 {
  font-size: 36px;
  color: #222;
  font-weight: bold;
}
.section-header p {
  font-size: 18px;
  color: #777;
  margin-bottom: 40px;
}
.team-members {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}
.team-member {
  width: calc(50% - 10px);
  text-align: center;
}
.team-member img {
  border-radius: 50%;
  margin-bottom: 15px;
}
.team-member h3 {
  font-size: 22px;
  margin: 10px 0;
}
.team-member p {
  font-size: 16px;
  color: #555;
}
.social-icons {
  margin-top: 15px;
}
.social-icons a {
  margin: 0 5px;
  font-size: 18px;
  color: #333;
}
.social-icons a:hover {
  color: #0077b5; /* LinkedIn color */
}
</style>

<!-- Schema for Customization -->
{% schema %}
{
  "name": "Our Team",
  "settings": [
    {
      "type": "text",
      "id": "team_title",
      "label": "Title",
      "default": "Our Team"
    },
    {
      "type": "textarea",
      "id": "team_description",
      "label": "Description",
      "default": "Meet our team of professionals."
    }
  ],
  "blocks": [
    {
      "type": "team_member",
      "name": "Team Member",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Team Member Image"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Full Name"
        },
        {
          "type": "text",
          "id": "job_title",
          "label": "Job Title",
          "default": "Job Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "Short bio or description."
        },
        {
          "type": "url",
          "id": "linkedin",
          "label": "LinkedIn URL"
        },
        {
          "type": "url",
          "id": "facebook",
          "label": "Facebook URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Our Team",
      "category": "Team"
    }
  ]
}
{% endschema %}
